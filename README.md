# Домашнее задание по лекции «Подготовка к собеседованиям».
___
## Задача 3
В ходе выполнение работы было выполнено:
* создан класс для работы с почтой;
* созданы методы отправки и получения писем:
    * в classes в строке 28 ```ms.sendmail(l,ms, msg.as_string())``` переработан в ```connect.sendmail(self.mail_login, email_to, msg.as_string())```, так как код не работал;
    * в classes в строке 42 ```email_message = email.message_from_string(raw_email)``` переработан в ```email_message = email.message_from_bytes(raw_email)```, так как raw_email возвращал байты, а не строку;
* Все значения определяются как аттрибуты класса, либо аргументы методов;
* Переменные и код соотвествуют PEP8;
* Инициализация класса и работа с экземпляром класса перенесены в конструкцию ```if __name__ == '__main__':```
### Если есть замечания, с радостью поправлю